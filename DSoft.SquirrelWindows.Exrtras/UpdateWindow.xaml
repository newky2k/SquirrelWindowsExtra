<Window x:Class="DSoft.SquirrelWindows.Exrtras.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DSoft.SquirrelWindows.Exrtras"
        mc:Ignorable="d"
               Title="Update Available" Height="225" Width="485" ResizeMode="NoResize" ShowInTaskbar="False" WindowStyle="ToolWindow">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="75" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">

        </Grid>
        <Border Grid.Row="0" BorderThickness="0,0,0,1" BorderBrush="Gray" Background="White">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Margin="10,5,0,0">
                    <Label FontWeight="Bold">Application Update</Label>
                    <TextBlock>A new version of {APP} is available. Do you want to download it now?</TextBlock>
                </StackPanel>
                <Grid Grid.Column="2" Margin="5,0,5,0">
                    <Image Width="64" Height="64" />
                </Grid>
            </Grid>
        </Border> 
        <Grid Grid.Row="1" VerticalAlignment="Center" Margin="10">
            <StackPanel HorizontalAlignment="Left">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Width="100" Margin="5">Current Version:</TextBlock>
                    <TextBlock Width="100" Margin="5">1.0.0.0</TextBlock>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Width="100" Margin="5">New Version:</TextBlock>
                    <TextBlock Width="100" Margin="5">1.0.0.0</TextBlock>
                </StackPanel>
                
            </StackPanel>
        </Grid>
        <Border Grid.Row="2" BorderThickness="0,1,0,0" BorderBrush="Gray">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Width="75" Margin="5" Click="OnOKButtonClicked">OK</Button>
                    <Button Width="75" Margin="5" Click="OnSkipButtonClicked">Skip</Button>
                </StackPanel>
            </Grid>
        </Border>

    </Grid>
</Window>
